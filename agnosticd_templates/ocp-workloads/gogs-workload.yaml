---
cloud_provider: none
env_type: ocp-workloads

ocp_username: ${{ inputs.openshift_user }}
guid: ${{ inputs.guid }}

ocp_workloads:
  - ocp-workload-gogs

# workload-specific vars
gogs_project: "{{ guid }}-gogs"

target_host: localhost
